from django.db import models


# TODO: Need models to store apps' variables and actions.
# TODO: Need a model to store created rules (or a single rule?)
# TODO: Need a way to query/run all existing rules (see diary.rules.run_rules)
